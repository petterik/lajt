(ns lime.core-test
  (:require [clojure.test :refer :all]
            [lime.core :refer :all])
  (:import (clojure.lang DynamicClassLoader)))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

{:query {}
 :pattern []}

DynamicClassLoader