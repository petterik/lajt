(ns lime.core-test
  (:require [clojure.test :refer [deftest is testing]]
            [lime.core :refer :all]
            [datascript.core :as ds]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
